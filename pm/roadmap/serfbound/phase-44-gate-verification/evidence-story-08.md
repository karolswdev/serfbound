# Evidence — SB-44-08 A Vertical Split and a Forged Results Window

## Vertical split + the forged HUD

`artifacts/deck-vertical-split.png` — a landscape viewport: the deck (the
protocol) fills the left column; the right column is the game panel with the
rig running and the gump-skinned HUD over it. The window is split vertically,
as asked.

`artifacts/rig-hud-gump.png` — the in-game results window, dressed in the
shell's materials: the carved brass `frame.png` border (9-slice) over
wood-under-ink, parchment/moss text in `--sb-font`, and the new PixelLab
`button.png` plate on every control. The active Pass verdict shows in
`--sb-meadow` with an inset ring.

## Design-standard conformance

```
npm run check:design
  → serfbound-design-tokens-ok: 44 tokens defined, 44 consumed, 0 reserved,
    raw-color ratchet 0/0.
```

The `.rig-hud*` rules use only `--sb-*` tokens (no raw colors), and the
`frame.png` / `wood.png` / `button.png` gump references all resolve on disk.

## Behaviour unchanged

```
verify-deck.mjs   → ALL DECK ASSERTIONS PASS (27 ok)  (split/open/close intact)
verify-rigs       → Node pass 11/11; browser pass: phase-35-lumberjack and
                    phase-36-road-split boot + HUD, reach Running
test:unit         → # pass 329  # fail 0
check:boundaries / check:independence / test:docs → all ok
```

## The asset

`packages/app/src/gumps/button.png` — 128×64, generated by PixelLab
(map-object, side view, high detail, single-color outline) to match the
existing `frame.png` palette: a brass-cornered parchment plate, chunky
pixels, no gradients. First-party, committed (design standard §8).
