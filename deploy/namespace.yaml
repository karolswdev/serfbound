# Serfbound owns exactly one namespace on the shared cluster
# (hosting-infrastructure decision record, SB-29-01).
apiVersion: v1
kind: Namespace
metadata:
  name: serfbound
  labels:
    app.kubernetes.io/part-of: serfbound
